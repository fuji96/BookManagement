<h1>books</h1>

<table class="table table-striped table-bordered" >
  <thead>
    <tr>
      <th>Title</th>
    </tr>
  </thead>

  <tbody>
    <% @user.bookdata.each do |book| %>
      <tr>
        <td><%= book.title %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', users_path %>
<br>
<%= link_to "Home", { :controller => 'top', :action => 'index'} %>
