<%= form_for(@bookdatum) do |f| %>
  <% if @bookdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookdatum.errors.count, "error") %> prohibited this bookdatum from being saved:</h2>

      <ul>
      <% @bookdatum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :authors %><br>
    <%= f.text_field :authors %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label :published %><br>
    <%= f.date_select :published %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
